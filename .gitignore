# Pascal source from https://github.com/mbek/elebbs (Q Public License).
# Published Go programs: cmd/elebbs, cmd/eleserv, cmd/elemail.

*.exe
*.test
*.out

# Untested — keep local, do not publish.
/cmd/elconfig/
/cmd/elefile/
/cmd/elemgr/
/EleBBS-Go/

# Tests leave DOS separators unmapped on non-Windows hosts, so they create
# files whose names contain literal backslashes. Never a real path here.
*\\*
